{
  "name": "birko-web-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Component gallery + live design-token editor + theme-CSS export for Birko.Web.*",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "puppeteer": "^25.11.0"
  }
}
